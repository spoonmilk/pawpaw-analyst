import { motion } from 'framer-motion';

export default function SarcasmToggle({ setToggle }: { setToggle: any }) {
  return (
    <div className="flex items-center justify-center mt-4 space-x-2">

    </div>
  );
}